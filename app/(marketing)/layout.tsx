// ============================================================
// (marketing) layout — wraps the landing page with Navbar/Footer.
// This is a Server Component.
// ============================================================

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

import React from "react";
